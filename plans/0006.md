# 0006 — SPEC-aligned realtime (text, voice, video)

| Field | Value |
|-------|--------|
| **Status** | in_progress |
| **SPEC** | [§8 WebRTC](../SPEC.md), [§9 Signaling](../SPEC.md), [§9.5 Community helpers](../SPEC.md), [§6.2 Noise](../SPEC.md) |

## Summary

Bring text, voice, and video to SPEC `0.2.0-draft` compliance: community ICE catalog, single `RTCPeerConnection` per peer, Noise XX over gossipsub, and connectivity hardening.

## Phases

### Phase 1 — Connectivity foundation

- [x] Seed `network-helpers.json` with Metered STUN + Open Relay TURN (`network_helpers.rs`, `bootstrap.rs`)
- [x] Remove proprietary Google STUN from `ice-config.ts` / `rtc-utils.ts`
- [x] `resetIceServerCache` + `recycleAllPeerConnections` on helper changes
- [x] Overlay vs Direct connectivity banners in chat thread

### Phase 2 — Single PC per peer

- [x] Unified [`peer-connection.ts`](../src/lib/transport/peer-connection.ts) (text DC + call tracks)
- [x] Hangup removes tracks, keeps PC for text
- [x] `text-peer.ts` / `call-peer.ts` re-export shims

### Phase 3 — Noise XX sessions

- [x] `noise_session.rs` unit test (snow `Noise_XX_25519_ChaChaPoly_SHA256`)
- [x] Gossipsub handshake on `vibe/noise/{conversation_id}` (`noise_overlay.rs`)
- [x] `ensure_session` Tauri command + frontend gate before transport/calls

### Phase 4 — Text hardening

- [x] `seq` on `WireChat` + replay guard
- [x] `set_peer_blocked` / `is_blocked` in store
- [ ] Typing / presence (deferred)
- [ ] CBOR envelopes (deferred per SPEC reference-client note)

### Phase 5 — Voice/video hardening

- [x] `restartIce` on ICE failed while connecting
- [x] Opus / VP9 codec preferences when supported
- [ ] Distinct call UI states for `IceFailed` / `NoRelay` (follow-up)

### Phase 6 — Verification

- [x] This plan document
- [ ] Manual two-client runbook (below)

## Test matrix

| Scenario | Text | Voice | Video |
| --- | --- | --- | --- |
| Same LAN | DC open, Direct badge | Call + text after hangup | Camera toggle |
| Cross-network (catalog TURN) | Gossipsub → DC upgrade | ICE via relay | Same |
| Network switch | `restartIce` on PCs | Call reconnect attempt | Same |
| Noise handshake | `ensure_session` before send | Before `call-invite` | Same |
| Reachability flap | Outbox flush, pending UI | N/A | N/A |

## Manual runbook

1. Delete app data on two devices (fresh catalog seed).
2. Join same room, add contacts, verify overlay reachable banner clears.
3. Send text — pending until reachable, then delivered ticks.
4. Voice call cross-network; hang up; send text on same thread without reconnect.
5. `cargo test noise_xx` in `src-tauri`.

## Build gates

```bash
cd src-tauri && cargo test noise_xx && cargo build
bun run build
```
